name: BUILD

on:
  push:
    branches: [ testing_file ]
  pull_request:
    branches: [ testing_file ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      
    - name: make all
      run:  make all -C Implementation/

